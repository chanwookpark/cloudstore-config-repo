info: 
 component: api-product
 profile: local

server:
 port: 8830

endpoints:
 restart:
  enabled: true

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
  healthcheck:
   enabled: true    