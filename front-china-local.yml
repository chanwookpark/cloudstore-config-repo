info:
 component: front-china
 profile: local

server:
 port: 8820

endpoints:
 restart:
  enabled: true

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
  healthcheck:
   enabled: true   